# Market fixtures

M2/M3 đặt golden normalized postings, extraction labels và aggregate expectations ở đây.
Fixture nhỏ, deterministic; dữ liệu seed phải ghi rõ `fictional=true`, snapshot thật phải có
source/hash/provenance và quyền sử dụng.

