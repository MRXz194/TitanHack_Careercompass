[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: isolated domain/schema tests without HTTP or network
    contract: API and cross-layer schema compatibility tests
    integration: tests spanning FastAPI routers, services, and local seed/storage
    e2e: full user-journey tests; excluded from normal CI unless explicitly selected

